## Devtools Part 2 Questions

1. What was the bug?
   The bug is that the function takes the two inputs as strings and then concatenates them instead of taking them as integers and summing them.
2. How would you fix it? 
   I would fix it by changing the num1 and num2 from strings to numbers and then adding them together.